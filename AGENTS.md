# Guidance for CPP repository

* The domain entity `Task` lives in `Models.Models.Task`. When working in this repository, always reference `System.Threading.Tasks.Task` with its full namespace to avoid confusion, and clarify explicitly when you mean the domain `Models.Models.Task`.
